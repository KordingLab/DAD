function [KL, g   = evalKLKDE_mo(W,X, ydim, p1)

xdim= size(X, 2);

WMat= reshape(W, xdim, ydim);
Yhat= X*WMat;
vy= var(Yhat);
p2= kde(Yhat', (vy/10)');
p2= ksize(p2, 'lcv');

KL= kld(p2,p1);



return;